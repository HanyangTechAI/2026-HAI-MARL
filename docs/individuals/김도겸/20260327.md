🎮 Arm 유형 1: 안정적인 캐시카우 (Stationary Gaussian - 3개) - 군집 갈등의 전장가장 수요가 많고 안정적이라 에이전트들이 초반에 가장 많이 몰려가 슬리피지(병목)를 겪게 될 핵심 라인입니다.현실 매칭: Arm 1 (CA_H1), Arm 2 (WI_H1), Arm 3 (TX_H1) (Hobbies 데이터 활용)실질 모델: sales_train_evaluation.csv의 HOBBIES_1 부서 5년 치 일일 판매량의 **전체 평균(Mean)**과 **분산(Variance)**을 그대로 정적 모델로 씁니다. (올려주신 CA 그래프의 brown line처럼 아주 평평하고 안정적입니다.)수학화:$$R_{t, arm} \sim \mathcal{N}(\mu_{arm}, \sigma_{arm}^2)$$$\text{Data Extract: }$ agg_df.groupby(['state_id', 'dept_id']).agg(['mean', 'var'])
🎮 Arm 유형 2: 이벤트형 롤러코스터 (Shock/Event - 4개) - 논문의 핵심리드의 지적을 해결하기 위한 가장 중요한 ARM들입니다. calendar.csv의 특정 날짜(d_1 ~ d_1941)가 되면 수요가 미친 듯이 폭발하는 함정 라인입니다.현실 매칭: Arm 4 (CA_F3), Arm 5 (WI_F3), Arm 6 (TX_F3), Arm 7 (CA_MULTI_SHOCK) (Foods_3 데이터 활용)실질 모델: 평소에는 조용하다가 추수감사절이나 슈퍼볼 날짜가 되면 수요가 **4.5배 폭증(Multiplier)**합니다. 특히 올려주신 Foods_3 그래프(red line)는 폭발하는 볼륨과 이벤트성 패턴을 완벽하게 보여줍니다. * 수학화 (T차에서 충격 발생):$$R_{t, arm} = \mathcal{N}(\mu_{baseline}, \sigma_{baseline}^2) \times \text{Multiplier}(t)$$$$\text{Multiplier}(t) = 1 \text{ (평소)}, \quad \text{Multiplier}(t) = N \text{ (t 가 이벤트 날짜일 때)}$$$\text{Data Extract: }$ calendar.csv에서 이벤트 날짜(shock_step)를 찾고, 해당 날짜의 판매량이 직전 7일 평균 대비 몇 배(N)로 뛰었는지 계산합니다.
🎮 Arm 유형 3: 장기 패러다임 변화 (Trend - 4개) - 장기 학습의 목표시간이 흐를수록 서서히 수요가 변하는 라인으로, 변화하는 환경(Non-stationary)을 AI가 얼마나 빨리 눈치채는지 평가하는 핵심 지표가 됩니다.현실 매칭: Arm 8 (CA_HO1), Arm 9 (WI_HO1), Arm 10 (TX_HO1), Arm 11 [가상의 우하향 사양산업] (Household_1 데이터 활용)실질 모델: 올려주신 CA 그래프의 Household_1(olive line)은 시간이 지날수록 꾸준히 우상향하는 추세(Trend)를 완벽하게 보여줍니다. 시작 수요는 낮지만 매 스텝 수요가 꾸준히 증가(slope $m > 0$)합니다.수학화:$$R_{t, arm} = \mu_{start} + m \cdot t + \mathcal{N}(0, \sigma^2)$$$\text{Data Extract: }$ linregress( np.arange(1941), agg_df['d_1':'d_1941'].sum() ) 로 start_mean과 slope 값을 뽑아냅니다.
🎮 Arm 유형 4: 극강의 노이즈/밈 주식 (Uniform/Volatile - 4개) - 리스크 테스트현실 매칭: Arm 12 (CA_HO2), Arm 13 (WI_HO2), Arm 14 (TX_HO2), Arm 15 [가상의 완전 무작위] (Household_2 데이터 활용)실질 모델: 올려주신 CA 그래프의 Household_2(cyan line)처럼 평균은 낮지만, 요동치는 노이즈가 엄청나게 커서 AI의 멘탈을 흔들어놓는 하이리스크-하이리턴 자산입니다. 0부터 N(최대값) 사이에서 완전히 무작위로 수익률이 결정됩니다.수학화:$$R_{t, arm} \sim \mathcal{U}(low, high)$$$\text{Data Extract: }$ 해당 라인의 5년 치 일일 판매량 중 최솟값(low)과 최댓값(high)을 설정합니다.